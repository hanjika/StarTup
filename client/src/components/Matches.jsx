import React from 'react';

const Matches = ({ id }) => {
  console.log(id);
  return (
      <section className='matches-section'>
          <h2>Your matches</h2>
      </section>
  );
};

export default Matches;
