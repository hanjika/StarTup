import React from 'react';

const Matches = ({ id }) => {
  return (
      <section className='matches-section'>
          <h2>Your matches {id}</h2>
      </section>
  );
};

export default Matches;
