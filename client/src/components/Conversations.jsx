import React from 'react';

const Conversations = ({ id }) => {
  return (
    <section className='conversation-section'>
        <h2>Your conversations {id}
        </h2>
    </section>
  );
};

export default Conversations;
